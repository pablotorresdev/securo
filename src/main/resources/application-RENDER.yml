spring:
  application:
    name: conitrack
  thymeleaf:
    cache: false
  datasource:
    # Uncomment this section to use H2 Database
    # url: jdbc:h2:mem:testdb
    # driverClassName: org.h2.Driver
    # username: sa
    # password:
    # h2:
    #   console:
    #     enabled: true
    url: postgresql://conitrack_user:xykmzdgGu9BITrWsOff2FFSkDmv5Fbey@dpg-d05c33euk2gs73cb0520-a/conitrack
    username: conitrack_user
    password: xykmzdgGu9BITrWsOff2FFSkDmv5Fbey
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        connection:
          charSet: UTF-8
          characterEncoding: UTF-8
          useUnicode: true
    show-sql: false
  devtools:
    livereload:
      enabled: true
    restart:
      additional-paths: src/main/resources
      additional-exclude: static/**
  web:
    resources:
      static-locations: classpath:/static/,classpath:/public/
#  sql:
#    init:
#      mode: always

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
  servlet:
    session:
      timeout: 300s

logging:
  level:
    org.springframework.security: DEBUG
    org.thymeleaf.extras.springsecurity6: DEBUG
    org.springframework.web: DEBUG
    org.springframework.boot.devtools: DEBUG
    org.springframework.jdbc.datasource.init: DEBUG